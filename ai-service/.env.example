# Required for CV parsing and LLM ranking (Groq). Get a key at https://console.groq.com/
GROQ_API_KEY=

# hybrid | embedding | llm
SCORER_MODE=hybrid

# Scorer blend weights (hybrid mode)
SCORER_EMBED_WEIGHT=0.4
SCORER_LLM_WEIGHT=0.4
SCORER_RULES_WEIGHT=0.2
